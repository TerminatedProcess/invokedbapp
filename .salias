clear
set -gx ENV_NAME '.venv'
alias rmenv='rm -rf .envrc $ENV_NAME uv.lock; exit'
alias mkenv='mkuv $ENV_NAME 3.12.10'
alias run='python invokedbapp.py'

function install
   clear
   uv pip install pip
   uv pip install -r requirements.txt
end
